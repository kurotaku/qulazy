<%= form_tag(judge_path(post.id), method: :delete, remote: true) do %>
  <%= hidden_field_tag :post_id, post.id %>
  <%= hidden_field_tag :type, 'Bad' %>
  <%= submit_tag 'Un bad', class: 'btn btn-danger' %>
<% end %>