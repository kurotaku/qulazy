<%= form_for(current_user.judges.build, remote: true) do |f| %>
  <%= hidden_field_tag :post_id, post.id %>
  <%= hidden_field_tag :type, 'Good' %>
  <%= f.submit 'Good', class: 'btn btn-success' %>
<% end %>