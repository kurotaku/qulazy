<h2>ユーザー一覧</h2>
<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user_path(user) %><br />
        @<%= user.username %><br />
        <%= user.email %>
        <hr />
      </li>
    <% end %>
  </ul>
<% end %>